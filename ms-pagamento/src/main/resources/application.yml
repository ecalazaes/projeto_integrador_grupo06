server:
  port: 8082

spring:
  application:
    name: ms-pagamento
  datasource:
    url: jdbc:mysql://localhost:3306/ms_pagamento
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    path: /api